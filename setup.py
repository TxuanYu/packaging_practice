from setuptools import setup, find_packages

setup(
    name = 'myproject', # same as your filename
    version = '0.1',
    description = 'first setup file',
    author = 'Txuan',
    packages = find_packages(),
    author_email = 'hohahi0@gmail.com'
)